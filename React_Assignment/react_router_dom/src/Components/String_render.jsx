const String_render = () => {
  const definition =
    "String Rendering means displaying (rendering) text (string) on the screen using code.";
  return (
    <section className="page">
      <h1>This String Rendering process</h1>
      <p>{definition}</p>
    </section>
  );
};

export default String_render;
