const Template_literals = () => {
  const definition =
    "Multi-line strings, insert variables inside strings, write cleaner readable string code. They use backticks → ` ` (NOT quotes).";

  return (
    <section className="page">
      <h1>Template Literals</h1>
      <h4>This is definition: ` {definition} `</h4>
    </section>
  );
};

export default Template_literals;
